services:
  - type: web
    name: cubevault-solver
    runtime: python
    rootDir: .
    buildCommand: pip install -r solver_service/requirements.txt
    startCommand: uvicorn solver_service.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"
